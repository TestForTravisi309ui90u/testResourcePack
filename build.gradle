apply plugin:  'maven'
apply plugin: 'java'

sourceSets {
    main {
        resources {
            srcDirs = ["."]
            include "**"
            exclude ".gitignore", ".github/**"
        }
   }
}
