apply plugin:  'maven'
apply plugin: 'java'

sourceSets {
    main {
        resources {
            srcDir = "."
            include "**"
            exclude ".gitignore", ".github/**"
        }
   }
}
